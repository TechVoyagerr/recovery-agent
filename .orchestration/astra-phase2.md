# PHASE 2 (hard cap 15 minutes): make the repo submission-grade. Work autonomously in /Users/macbook/Documents/VSC/Razorpay. Read .orchestration/astra-phase1-report.md and README.md first.
OWNERSHIP: you may edit src/lib/**, src/app/api/**, prisma/**, scripts/**, README.md, .gitignore, .env.example, package.json, docs/**. Do NOT edit src/app/(non-api pages), src/app/layout.tsx, src/app/globals.css, src/components/** (a designer is editing them right now). Do not stop or restart any dev server on port 3000 (designer uses it); use a different port or the test DB for your own checks.

1. STOP / opt-out handler: `POST /api/inbound` accepting `{from, text}` (and a Twilio/WhatsApp-style form body) where text matching STOP/UNSUBSCRIBE/BAND (case-insensitive) marks the customer optedOut, cancels PENDING attempts, logs an AgentEvent "OPT_OUT". Rule engine must never message an opted-out customer (verify with a test). Add a vitest for it.
2. Honesty + attribution: ensure a transaction is only counted "recovered" when a RecoveryAttempt's payment link/retry is what got paid (attribution field), not when the original payment later succeeds. Label simulator output as synthetic in API responses (`synthetic: true` in stats when data came from a simulation run).
3. Secrets: confirm `.env.local` is gitignored, `.env.example` is NOT ignored and lists all vars with placeholders, grep the whole tree (excluding node_modules, .next) for "[REDACTED_OPENROUTER_PREFIX]" and the OpenRouter key prefix; nothing must remain outside .env.local. Also delete any `/tmp`-style logs referenced in repo.
4. README.md rewrite to be judge-skimmable, concise, zero em dashes (use commas or periods), no emoji: one-line value proposition; 3-line "Why this wins" (reason-specific recovery not blind retry, consent-aware and no auto-debit, learns per reason x channel x timing); Quickstart (exact commands, one block); Architecture (mermaid, small); How the agent decides (rule engine primary, LLM only polishes copy, be explicit); Reproducible batch result table from `npm run simulate -- --n 1000 --seed 42` (failed, attempted, suppressed, recovered, rate, rupees recovered, per-reason rows) clearly labelled synthetic; One full trace example (failure → diagnosis → decision with reasoning → link → nudge → outcome) and one refusal example (opted-out customer → no message); Razorpay products used; API reference (short table); Implemented vs simulated vs limitations (honest); Tests (`npm test` output summary). Add a `docs/screenshots/` folder placeholder note that screenshots are added by the design pass, do not fabricate them.
5. Scripts: `npm run setup` = install + prisma push + seed; `npm run demo` = setup + simulate 1000 seed 42. `npm run build` must pass (run it; if the designer's in-progress UI files break the build, note it in the report but do not edit them).
6. Remove every em dash ("—") from files you own (src/lib, scripts, README, prisma). Use commas, colons or periods.
7. git: `git init` (if not already), sensible .gitignore, `git add -A`, verify `git status` shows no .env.local and no secrets, commit "Recovery Agent: autonomous failed-payment recovery for Razorpay" with author from git config. Do not push.
Report to .orchestration/astra-phase2-report.md: what changed, build/test results, git status summary, anything left.
